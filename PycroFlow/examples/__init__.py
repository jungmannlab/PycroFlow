"""Runnable examples and demo data for PycroFlow.

Nothing in this subpackage is imported by the main package at runtime — it
exists so that the REPL examples shown in the module docstrings, the test
fixtures, and the quickstart docs have one obvious source of truth instead
of being scattered as module-level demo constants in orchestration.py.
"""
